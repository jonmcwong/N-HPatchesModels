This project is broken down into 2 segments. Denoising and a descriptor generation. 

The trained denoising model will be exported to the working directory as "denoising_model.h5".
The descriptor generator will load it. 
The trained descriptor model will be exported to the working directory as "descriptr_model.h5"
